name: Build and Release

on:
    push:
        branches:
            - main

jobs:
    build-and-release:
        runs-on: windows-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Setup .NET
              uses: actions/setup-dotnet@v4
              with:
                  dotnet-version: 9.x
            - name: Build
              run: dotnet publish -r win-x64 -c Release -o output_dir
            - name: Get Version
              id: get_version
              shell: pwsh
              run: |
                  $version = Select-String -Path (Get-ChildItem -Path **/*.csproj) -Pattern '<AssemblyVersion>(.*?)</AssemblyVersion>' | ForEach-Object { $_.Matches.Groups[1].Value }
                  echo "Version extracted: $version"
                  echo "version=$version" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8
            - name: Create Release
              uses: softprops/action-gh-release@v2
              with:
                  tag_name: v${{env.version}}
                  name: Release v${{env.version}}
                  draft: false
                  prerelease: false
                  files: output_dir/*.exe
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
